---
layout: page
title: Cloud-Agnostic Multi-Modal Model Serving Engine 
date: 2026-08-16
category: work
giscus_comments: false
---

[![Kubernetes](https://img.shields.io/badge/Kubernetes-K3s%20%7C%20EKS-blue?logo=kubernetes)](https://kubernetes.io/)
[![Triton Inference Server](https://img.shields.io/badge/NVIDIA-Triton_24.08-green?logo=nvidia)](https://developer.nvidia.com/nvidia-triton-inference-server)
[![vLLM](https://img.shields.io/badge/LLM-vLLM_Backend-orange)](https://github.com/vllm-project/vllm)
[![License](https://img.shields.io/badge/License-MIT-purple.svg)](LICENSE)


The general consensus online is that running local models has become trivial. That is certainly true if the goal is running a single-user `cli` wrapper like `ollama run` in a terminal window. However, that abstraction falls apart the moment one need dynamic batching, heterogenous multi-model execution, optimized GPU memory lifecycle management, or the ability to manage host CUDA drivers predictably without breaking local virtual environments.

Rather than cluttering the host `os` with ad-hoc `python` scripts or standard homelab setups (I am not interested in running a media server), I began building a local `k3s` environment leveraging NVIDIA Triton Inference Server with `vLLM` and `ONNX` Runtime backends. The goal is to establish a local edge execution environment that mirrors the exact containerized patterns, declarative configurations, and model registry topologies required for production deployments on Amazon `EKS`. For detailed technical steps, check out the [README](https://github.com/krishpn/miscellaneous/blob/main/README.md).


```text
+---------------------------------------------------------------------------------------------------+
|                                 LOCAL K3S CLUSTER (RTX 3070 Ti)                                   |
|                                                                                                   |
|  [ Host Network Ingress / NodePort Routing (Ports 30800 - 30802) ]                                |
|                                   │                                                               |
|                                   ▼                                                               |
|  [ Triton Inference Server Pod (triton namespace) ]                                             |
|   ├── Container Runtime: Containerd (runtimeClassName: nvidia)                                 |
|   ├── IPC Shared Memory: /dev/shm (Mounted emptyDir via RAM)                                  |
|   │                                                                                               |
|   ├── ONNX Runtime Backend  --> bge_small_embedding  (Static / Loaded on Boot)                   |
|   ├── ONNX Runtime Backend  --> resnet50_vision      (Static / Loaded on Boot)                   |
|   └── vLLM C++ Backend      --> qwen_1b / LLMs      (Dynamic --model-control-mode=explicit)   |
|                                                                                                   |
|  [ Shared VRAM Pool (8 GB) ] <-- VRAM Isolation & Dynamic Reclaim (/v2/repository/.../unload)   |
+---------------------------------------------------------------------------------------------------+
                                                │
                                 [TODO: Kustomize Overlays / S3 Sync]
                                                │
                                                ▼
+---------------------------------------------------------------------------------------------------+
|                                 CLOUD TARGET (AWS EKS) [TODO]                                     |
|  - [TODO] Triton + vLLM Multi-GPU Scaling (p4/g5 instance node groups)                            |
|  - [TODO] S3 Model Registry (s3://<BUCKET>)                                                |
|  - [TODO] ArgoCD GitOps Continuous Deployment (deploy/overlays/aws-eks)                         |
+---------------------------------------------------------------------------------------------------+
```

**Summary of execution steps & milestones**

**Step 1: Base `k3s` Infrastructure & GPU Passthrough Setup**

- Provision a single-node `k3s` cluster on the host system.

- Configure `containerd` (`/etc/containerd/config.toml`) to utilize `nvidia-container-runtime`.

- Register `runtimeClassName`: `nvidia` within `k3s` and verify `GPU` passthrough via [nvidia.com/gpu](https://nvidia.com/gpu) device limits.

**Step 2: Pod Shared Memory & Storage Architecture**

- Configure Kubernetes pod specs to mount `emptyDir` with medium: Memory to `/dev/shm` to support high-throughput IPC for PyTorch and ONNX worker processes.

- Set up host path mounts for local model weight storage (`/opt/model_repository`) to mimic a remote bucket structure.

**Step 3: Multi-Backend Triton Configuration**

- Build declarative `config.pbtxt` definitions for ONNX Runtime models (`bge_small_embedding`, `resnet50_visio`n) enabling dynamic batching (max_queue_delay_microseconds: 5000).

- Configure the `vLLM C++` backend for sequence generation (`qwen_1b`) leveraging PagedAttention and continuous batching.

**Step 4: Explicit Memory Isolation & Dynamic Model Control**

- Launch Triton with `--model-control-mode=explicit` to prevent vLLM from pre-allocating GPU VRAM at container initialization.

- Validate static loading of lightweight ONNX models on server startup (`~1.8 GB` VRAM footprint).

- Test dynamic loading and unloading of LLM weights via REST API endpoints (`POST /v2/repository/models/qwen_1b/load` and `/unload`) to manage VRAM allocation cleanly.

**Step 5: External Routing & Ingress Verification**

- Expose Triton ports via a NodePort service mapping host ports `30800` (`HTTP`), `30801` (`gRPC`), and `30802` (`Prometheus Metrics`).

- Perform streaming inference and embedding requests via standard HTTP/gRPC clients to confirm low-latency routing directly into the cluster.



**Cloud Migration Roadmap [TODO]**

[TODO] Step 6: Kustomize Overlays & S3 Synchronization: Abstract local host paths into Kustomize base manifests and write sync pipelines targeting `s3://<bucket>`.

[TODO] Step 7: EKS GPU Node Group Provisioning: Terraform multi-GPU node groups (`g5/p4 instances`) on AWS EKS with the AWS ALB Ingress Controller.

[TODO] Step 8: `GitOps` Automation with `ArgoCD`: Wire `deploy/overlays/aws-eks` into an ArgoCD application for automated cluster deployment.


### More

[Detailed Technical Steps](https://github.com/krishpn/miscellaneous/blob/main/README.md)